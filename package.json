{
  "name": "pr-trivia-checker",
  "version": "1.0.0",
  "description": "GitHub Action para bloquear PRs hasta completar trivia de validación",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "format": "prettier --write src\\*.ts",
    "lint": "eslint src/**/*.ts",
    "package": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run format && npm run lint && npm run build",
    "dev:success": "npm run build && node scripts/dev-test.js success",
    "dev:pending": "npm run build && node scripts/dev-test.js pending",
    "dev:invalid": "npm run build && node scripts/dev-test.js invalid",
    "dev:expired": "npm run build && node scripts/dev-test.js expired"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-org/pr-trivia-checker.git"
  },
  "keywords": [
    "actions",
    "github",
    "pull-request",
    "validation",
    "trivia"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "@typescript-eslint/eslint-plugin": "^6.15.0",
    "@typescript-eslint/parser": "^6.15.0",
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.56.0",
    "eslint-plugin-github": "^4.10.1",
    "jest": "^29.7.0",
    "prettier": "^3.1.1",
    "typescript": "^5.3.3"
  }
}
